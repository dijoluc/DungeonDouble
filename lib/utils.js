var utils = {

//for each loopt door een array van objects en voor elk object/parameter
centerGameObject : function (objects) {
 objects.forEach(function (object) {
      object.anchor.setTo(0.5);
    })
  }

//
// GameObject : function (objects) {
//  objects.forEach(function (object) {
//       object.anchor.setTo(0.5);
//     })
//   }

 };

